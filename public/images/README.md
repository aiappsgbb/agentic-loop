# Scenario images

Drop scenario JPEGs in this folder. Filenames must match the image field in src/data/scenarios.json
(without the images/ prefix — e.g. ccelerate-drug-development-with-ai.jpg).

These files are served from the site root by Vite (e.g. /images/<file>.jpg).
